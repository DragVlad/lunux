```bash
cat > file.txt # ввод в новый файл
cat file1.txt > file2.txt # перезаписывает/создает файл
cat file1.txt >> file2.txt # дописывает(с новой строки)/создает новый файл
ls --help > helpfile.txt # направляет поток вывода команды в файл

grep found_word < filename.txt
grep ipv4 < sysctl.conf

cat file1.txt && echo "\nTHE TEXT READ" # && если первая команда успешно отработала выполняет следующую
cat file1.txt && cat file6.txt || echo "BAD FILE" # || если первая команла не отработала, выполняет вторую

history 
```